version: 0.1
files:
  - source: deploy.sh
    destination: /tmp/deploy
  - source: wrapper.sh
    destination: /tmp/deploy
scripts:
  AfterDeploy:
    - location: /tmp/deploy/wrapper.sh
      runas: ubuntu